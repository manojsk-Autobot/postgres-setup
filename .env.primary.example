# PostgreSQL Primary Server Environment Variables
# Copy this file to .env.primary and update with your actual values
# DO NOT commit .env.primary to version control!

# Database Configuration
POSTGRES_DB=awx
POSTGRES_USER=awx
POSTGRES_PASSWORD=change_this_password

# Replication User Configuration
REPLICATION_USER=replicator
REPLICATION_PASSWORD=change_this_replication_password

# Network Configuration
STANDBY_IP=192.168.1.101
